node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Tesla key material — NEVER commit the private key
secrets/
# tesla-http-proxy local TLS material (generated during telemetry onboarding)
proxy-tls-*.pem

# Cloudflare local dev secrets
.dev.vars
.wrangler/

# Deployment-specific config (copy from wrangler.jsonc.example and fill in).
# Kept out of git so your domain / project ids / keys aren't published.
wrangler.jsonc

# IDE + local agent/skill caches
.idea/
.claude/
.agents/

# Local working docs. The docs/ folder itself stays tracked so future
# public-facing docs (and docs/screenshots/, used by the README) stay
# committable; only the current working/planning material is ignored.
docs/brainstorming/
docs/specs/
docs/guides/
docs/design/
